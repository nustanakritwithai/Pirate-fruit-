// S10: นิยามเควสต์ย้ายไป shared/src/quest/definitions.ts (single source of truth
// ระหว่างเกมกับ Server) — ไฟล์นี้คง re-export ให้ import เดิมทั้งหมดใช้ต่อได้
export { QUEST_DEFINITIONS, QUESTS_BY_ID } from '@pirate-fruit/shared';
