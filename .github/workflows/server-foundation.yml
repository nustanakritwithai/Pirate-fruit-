name: Client and server gates

on:
  pull_request:
  push:
    branches:
      - claude/blox-fruits-three-js-roadmap-1f2f5u

concurrency:
  group: server-gates-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    services:
      postgres:
        image: postgres:16-alpine
        env:
          POSTGRES_DB: pirate_fruit_test
          POSTGRES_USER: pirate_fruit
          POSTGRES_PASSWORD: pirate_fruit_test
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U pirate_fruit -d pirate_fruit_test"
          --health-interval 5s
          --health-timeout 5s
          --health-retries 10
    env:
      DATABASE_TEST_URL: postgresql://pirate_fruit:pirate_fruit_test@127.0.0.1:5432/pirate_fruit_test
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
      - run: npm ci
      - run: npm run build
      - run: npm run smoke:server
      - run: npm test
